import { Product } from '../../product.type';

const productPayload: Product = {
  category: 'category', imageUrl: 'imageUrl', name: 'name', price: 10,
};

const productData = { productPayload };

export default productData;
